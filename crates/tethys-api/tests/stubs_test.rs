//! Stub default coverage, split by namespace so a chunk adding a namespace
//! method edits only its own file. Every `Unimplemented` string is asserted
//! verbatim; the `MinimalApi` fixture lives in `stubs/mod.rs`.

mod stubs;
