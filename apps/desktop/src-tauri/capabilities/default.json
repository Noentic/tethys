{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "S0.0 minimum plus Wave 2 plugin permissions: OS notifications (M1.8) and the native folder picker (M1.16).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "dialog:default"
  ]
}
