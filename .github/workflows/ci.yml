name: ci

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  check:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Linux webview deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libgtk-3-dev libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev patchelf

      - uses: pnpm/action-setup@v4
        with:
          version: 10.17.0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - uses: Swatinem/rust-cache@v2

      - run: pnpm install --frozen-lockfile
      - run: pnpm exec turbo run codegen lint typecheck test --concurrency 100%
      - run: pnpm exec turbo run //#cargo:check //#cargo:test --concurrency 100%

      # Draft ACP v2 and the mock vendor stay feature-gated; prove both states
      # on every OS (M1.2 D16).
      - name: ACP v2 feature tests
        run: |
          cargo test -p tethys-acp --features acp-v2,mock
          cargo test -p tethys-agent-servers --features acp-v2
          cargo test -p tethys-core --features acp-v2
