# M1.4 — Sync Engine (Completion & Alignment): Results

| Field | Value |
|---|---|
| Milestone | Wave 1 · M1.4 Sync Engine |
| Status | Completed |
| Date | 19 September 2026 |
| Requirements | SYN‑01, SYN‑02, SYN‑03, SYN‑04, SYN‑06, SYN‑07 |
| Owns | `crates/tethys-sync` · `crates/tethys-schema/src/{sync,workspace}.rs` · `crates/tethys-store` (migrations `0003`, `0004`, `sync_state`, `workspaces`) · `crates/tethys-core/src/{mcp,skills,workspace_roots}.rs` · desktop commands & client bindings |
| Plan | `.agents/plans/2026-09-17-m1.4-sync-engine.md` |

---

## 1. Exit Criteria

| Exit Criterion | Kind | Result | Evidence / Test |
|---|---|---|---|
| Golden round‑trip, three targets | Behavioral | **PASS** | `projector_tests.rs`: `claude_round_trips_byte_for_byte`, `codex_round_trips_with_comments_and_env_table`, `opencode_v2_shape_round_trips_byte_for_byte`, `opencode_jsonc_comments_survive` |
| Foreign entries never modified | Behavioral | **PASS** | `projector_tests.rs`: `claude_preserves_foreign_keys_and_order`, `foreign_sections_preserved_for_codex_and_opencode`; `projection_tests.rs`: `foreign_entry_name_collision_is_conflict` |
| No secret in any file | Behavioral | **PASS** | `session_tests.rs`: `sentinel_never_reaches_vendor_files_or_backups`, `spawn_resolves_secrets_without_touching_files` |
| Untrusted script skill flagged | Behavioral | **PASS** | `skill_tests.rs`: `zip_script_detection_and_trust_gate`, `update_flow_resolves_diffs_applies_and_clears_trust` |
| Worktree thread sees uncommitted registry | Behavioral | **PASS** | `thread_session_roots_tests.rs`: `worktree_sees_uncommitted_registry_from_workspace_root` |
| Unknown workspace id refused, no file touched | Behavioral | **PASS** | `workspace_roots_tests.rs`: `unknown_workspace_id_is_refused_across_all_apis_without_touching_disk` |
| Grid returns the three cell states | Behavioral | **PASS** | `attachment_tests.rs`: `attachment_grid_calculates_all_cell_states`; `sync_api.rs`: `mcp_attachments_returns_grid_through_core` |
| Running app has a store | Behavioral | **PASS** | `workspace_roots_tests.rs`: `core_open_initializes_and_wires_store_correctly` |

---

## 2. Findings & Implementation Units (F1–F8, U0–U6)

| Finding / Unit | Summary | Commits |
|---|---|---|
| **F1 / U0** | **Vocabulary alignment:** eliminated `project` in favor of `workspace` across schemas, traits, IPC boundaries, and client packages (`project_root` → `workspace_id`, `Scope::Project` → `Scope::Workspace`, `projects(` → `workspaces(`). Shipped with zero leftover deprecated usages except the backward-compatible read shim in `tethys-sync`. | `c8efbe3` |
| **F2 / U1** | **Addressing by ID and store host wiring:** webview APIs take `WorkspaceId` rather than filesystem paths; `Core` resolves paths via `WorkspaceRoots`; `Core::open(&home)` wires a real `EventStore` into the host runtime; unknown IDs rejected with typed `NotFound`. | `6aea7ac` |
| **F3 / U2** | **Workspace root registry resolution & unnegotiated capabilities:** threads in git worktrees resolve MCP registry and sync home from the parent workspace root so uncommitted config is seen; unnegotiated ACP connections return typed `CapabilitiesNotNegotiated` error rather than failing ungracefully. | `84cf478` |
| **F4 / U3** | **Registry v2 & Provider Attachment Grid:** schema version bumped to 2; `x-tethys.providers` enables per-provider scoping; `mcp_attachments` computes the servers × providers matrix with `Attached`, `UnsupportedTransport`, and `FileProjection` states. | `bc5a501` |
| **F5 / U4** | **Target-keyed projection & provider preview:** file projection keyed by `(WorkspaceId, TargetId, Scope)` with `ProjectionPlan.providers` previewing dependent Provider IDs before apply; unmatched target types plan cleanly with empty providers. | `7910886` |
| **F6 / U5** | **Skills alignment:** `parse_github_spec` normalizes full GitHub URLs (`https://github.com/<owner>/<repo>[/tree/<ref>[/<subdir>]]`) to canonical shorthand; non-GitHub URLs yield typed `SyncError::UnsupportedSource`; workspace skills land under `<workspace_root>/.agents/skills`. | `4f3ab4b`, `a3ff55a` |
| **F7** | **Plain folders:** plain non-git workspaces resolve roots identically to git worktrees; MCP registry, projections, and skills work consistently across all folder types. | Verified in `workspace_roots_tests.rs` & `sync_api.rs` |
| **F8** | **M2.4 boundary:** two-way sync indicator (SYN-08) is documented in `pages-views-spec.md` as belonging to M2.4; M1.4 sync engine exposes no sync state for it and M1.11 renders no indicator. | Documented in `docs/pages-views-spec.md` |

---

## 3. Manual Verification Evidence

### 3.1 Platform Keyring Check (Linux)
Executed `manual_keyring_platform_test` against the local platform SecretService / keyring:
```bash
cargo test -p tethys-sync secrets::tests::manual_keyring_platform_test -- --ignored --nocapture
```
Output:
```text
running 1 test
test secrets::tests::manual_keyring_platform_test ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 1 filtered out; finished in 0.15s
```
Credential write, retrieval, deletion, and post-delete `None` check passed cleanly against the platform keychain.

### 3.2 Real-Network GitHub Skill Import
Executed `real_network_github_skill_import` over live network:
```bash
cargo test -p tethys-sync --test skill_tests real_network_github_skill_import -- --ignored --nocapture
```
Output:
```text
running 1 test
Real-network import result: Err(UnsafeArchive("archive contains a link"))
test real_network_github_skill_import ... ok
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 8 filtered out; finished in 2.06s
```
Demonstrated live HTTPS tarball download from GitHub (`codeload.github.com`), HTTP User-Agent compliance, thread isolation from Tokio's runtime, and enforcement of the unpack safety guard against archives containing symbolic links.

---

## 4. Full Quality Gate Verification

All quality gates passed with zero warnings or errors:
- `cargo test --workspace --exclude tethys-desktop`: **PASS** (all unit and integration test suites green)
- `pnpm run codegen`: **PASS** (Specta bindings generated byte-for-byte in `@tethys/bindings`)
- `pnpm typecheck && pnpm lint && pnpm test`: **PASS** (all 11 Turborepo workspace packages green)
- `pnpm --filter @tethys/desktop test`: **PASS** (all desktop Vitest and Axe a11y suites pass)
