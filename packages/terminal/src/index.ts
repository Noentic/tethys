//! `@tethys/terminal` — display-only agent terminal (M1.7 U7).

export {
  nextTerminalDelta,
  sunkenWellTheme,
  TerminalView,
  type TerminalViewProps,
  terminalOptions,
} from "./xterm-surface";
